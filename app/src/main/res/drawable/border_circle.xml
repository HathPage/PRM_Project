<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Lớp ngoài cùng (nhạt nhất) -->
    <item>
        <shape android:shape="oval">
            <solid android:color="@android:color/transparent" />
            <stroke android:width="3dp" android:color="#AAE0E0E0" /> <!-- Màu nhạt -->
        </shape>
    </item>

    <!-- Lớp thứ 2 (nhạt vừa) -->
    <item
        android:left="3dp"
        android:right="3dp"
        android:top="3dp"
        android:bottom="3dp">
        <shape android:shape="oval">
            <solid android:color="@android:color/transparent" />
            <stroke android:width="4dp" android:color="#AAC0C0C0" /> <!-- Màu vừa -->
        </shape>
    </item>

    <!-- Lớp trong cùng (đậm nhất) -->
    <item
        android:left="7dp"
        android:right="7dp"
        android:top="7dp"
        android:bottom="7dp">
        <shape android:shape="oval">
            <solid android:color="@android:color/transparent" />
            <stroke android:width="5dp" android:color="#AA808080" /> <!-- Màu đậm -->
        </shape>
    </item>

</layer-list>